// frontend/src/pages/EndpointConfig.jsx
import React from 'react';

const EndpointConfig = () => {
  return (
    <div className="page-container">
      <h2>API Endpoint Configuration</h2>
      <p>Use this form to define and manage the external APIs you wish to benchmark.</p>
      {/* Form fields for Endpoint ID, Name, Base URL go here */}
    </div>
  );
};

export default EndpointConfig;